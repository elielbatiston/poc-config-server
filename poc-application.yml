server:
  port: 8087
  servlet:
    context-path: '/poc-application'

management:
  endpoints:
    web:
      exposure:
        include:
          - refresh
          - health
          
application:
  ttl: 3000
  retry: 4